# Netflix style website
### Learning React

    yarn install or npm install

    then yarn dev or npm start dev to start the developement build

make sure to replace sample.env with correct values and rename appropriately